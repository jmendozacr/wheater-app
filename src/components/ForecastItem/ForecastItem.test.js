import React from 'react';
import ForecastItem from './ForecastItem';
import { findByText, render } from '@testing-library/react'

test('should render ForecastItem', async () => {
    // const { findAllByRole } = render(<ForecastItem weekday="monday" hour={12} temperature={3} state={["rain"]} />);

    // const forecastItem = await findAllByRole("heading");

    // const weekDayExample = await findByText("monday");

    // expect(weekDayExample).toHaveTextContent("monday");
    // expect(forecastItem[1]).toHaveTextContent("12");
    // expect(forecastItem[3]).toHaveTextContent("3");
});

